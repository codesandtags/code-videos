// ¿Cuál es el resultado esperado?

const bitcoins = [1, 2, 3, 4, 5];

bitcoins.splice(0, 2);
bitcoins.slice(0, 2);

bitcoins;

// Recuerda que el método "splice"
// extrae los valores del arreglo
// modificando su valor original.
// Por otro lado "slice" te 
// devuelve una copia.